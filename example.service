#!/bin/bash
SCRIPT="$(readlink -f "$0")"
DIR="$(dirname "$(readlink -f "$0")")"
cd $DIR
. $DIR/dcs/tools/service-runner/tmux-launch
launch-service $(calc-session-name $SCRIPT)

# run commands in tmux session
run-in-tmux "top"
run-in-tmux "ls -la"
